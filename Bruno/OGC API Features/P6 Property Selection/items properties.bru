meta {
  name: items properties
  type: http
  seq: 2
}

get {
  url: {{serviceHost}}/collections/{{dataset}}/items?properties=fid,bouwjaar
  body: none
  auth: inherit
}

params:query {
  properties: fid,bouwjaar
}

settings {
  encodeUrl: true
  timeout: 0
}
