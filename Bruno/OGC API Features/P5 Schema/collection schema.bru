meta {
  name: collection schema
  type: http
  seq: 1
}

get {
  url: {{serviceHost}}/collections/{{dataset}}/schema?f=json
  body: none
  auth: inherit
}

params:query {
  f: json
}

settings {
  encodeUrl: true
  timeout: 0
}
