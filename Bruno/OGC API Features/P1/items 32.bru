meta {
  name: items 32
  type: http
  seq: 11
}

get {
  url: {{serviceHost}}/collections/{{dataset}}/items/{{featureId}}
  body: none
  auth: inherit
}

headers {
  Accept: application/geo+json
}

settings {
  encodeUrl: true
  timeout: 0
}
