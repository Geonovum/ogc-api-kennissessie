meta {
  name: feature delete
  type: http
  seq: 8
}

delete {
  url: {{serviceHost}}/collections/{{dataset}}/items/{{featureId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
